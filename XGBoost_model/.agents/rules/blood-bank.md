---
trigger: always_on
---

This is a MERN stack application. Use functional React components with Hooks. Use standard Bootstrap for UI styling to match the existing app. For the ML microservice, use Python 3 with Flask or FastAPI. Always include proper try/catch error handling and react-toastify for frontend alerts.